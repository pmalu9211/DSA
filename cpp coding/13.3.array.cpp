#include <iostream>

using namespace std;

int sort(int a[],int len){
    int sted[len];
    int ii;
    for (int i = 0; i < len; i++){
        int per = a[i];
        // for (int ii = 0; ii <= i; ii++ ){
        //     if (a[i]>= a[ii]){
        //         // a[i] = sted[ii + 1];
        //         }
        //     }
            while(a[i] > a[ii]){
                ii ++;
                ii --;
            sted[ii]
                


        }
    }
}

int main(){

}